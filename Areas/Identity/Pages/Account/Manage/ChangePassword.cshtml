@page
@model ChangePasswordModel
@{
    ViewData["Title"] = "Đổi mật khẩu";
    ViewData["ActivePage"] = ManageNavPages.ChangePassword;
}

<h3 class="mb-4 text-gradient fw-bold">@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-8">
        <form id="change-password-form" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3 rounded-3 p-3 border border-danger-subtle bg-danger-subtle" role="alert"></div>
            
            <div class="mb-4">
                <label asp-for="Input.OldPassword" class="form-label fw-medium">Mật khẩu hiện tại</label>
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-lock"></i></span>
                    <input asp-for="Input.OldPassword" class="form-control form-control-lg" autocomplete="current-password" aria-required="true" />
                </div>
                <span asp-validation-for="Input.OldPassword" class="text-danger"></span>
            </div>
            
            <div class="mb-4">
                <label asp-for="Input.NewPassword" class="form-label fw-medium">Mật khẩu mới</label>
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-key"></i></span>
                    <input asp-for="Input.NewPassword" class="form-control form-control-lg" autocomplete="new-password" aria-required="true" />
                </div>
                <span asp-validation-for="Input.NewPassword" class="text-danger"></span>
                <div class="form-text mt-2">
                    <i class="fas fa-info-circle me-1 text-primary"></i>Mật khẩu phải có ít nhất 6 ký tự, bao gồm chữ cái và số.
                </div>
            </div>
            
            <div class="mb-4">
                <label asp-for="Input.ConfirmPassword" class="form-label fw-medium">Xác nhận mật khẩu mới</label>
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-check-circle"></i></span>
                    <input asp-for="Input.ConfirmPassword" class="form-control form-control-lg" autocomplete="new-password" aria-required="true" />
                </div>
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>
            
            <button type="submit" class="btn btn-primary px-4 py-2">
                <i class="fas fa-save me-2"></i>Cập nhật mật khẩu
            </button>
        </form>
    </div>
    
    <div class="col-md-4">
        <div class="card border-0 shadow-sm bg-light">
            <div class="card-body p-4">
                <h5 class="fw-bold mb-3">
                    <i class="fas fa-shield-alt me-2 text-primary"></i>Lưu ý bảo mật
                </h5>
                <ul class="mb-0 ps-3">
                    <li class="mb-2">Tạo mật khẩu mạnh có ít nhất 8 ký tự</li>
                    <li class="mb-2">Kết hợp chữ hoa, chữ thường, số và ký tự đặc biệt</li>
                    <li class="mb-2">Không sử dụng thông tin cá nhân trong mật khẩu</li>
                    <li>Không sử dụng lại mật khẩu từ các tài khoản khác</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<style>
    .text-gradient {
        background: linear-gradient(45deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
</style>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
